export enum ProviderStatusEvents {
  CONNECTED = "connected",
  DESTROY = "destroy",
  DISCONNECT = "disconnect",
}

export enum RealtimeChannelState {
  DISCONNECTED = "DISCONNECTED",
  CONNECTED = "CONNECTED",
  CONNECTING = "CONNECTING",
}
